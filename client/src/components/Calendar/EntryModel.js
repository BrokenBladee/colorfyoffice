import React, {Component} from 'react'
import {connect} from 'react-redux'
import {addItem} from '../actions/itemActions'

class EntryModel extends Component{
  
}

export default connect()(EntryModel)